#ifndef TYPES_H
#define TYPES_H

typedef enum {
    SELECT_NONE,
    SELECT_CUBE,
    SELECT_SPHERE,
    SELECT_PYRAMID,
    SELECT_CYLINDER,
    SELECT_PLANE,
    SELECT_MODEL
} SelectedType;

#endif
